# gradle.properties
# 项目配置文件
# 依赖版本管理已迁移到 gradle/libs.versions.toml

# Gradle 构建配置
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Kotlin DSL 支持（如果需要）
kotlin.code.style=official